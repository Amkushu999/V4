<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 🛡️ CRITICAL FOR ANDROID 11+ (API 30): Allows the app to see installed apps for the Picker/HideApp -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
    </queries>

    <!-- Network & System Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Storage Permissions (Handles Android 12 and below) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="29" />
        
    <!-- 🛡️ CRITICAL FOR ANDROID 13+ (API 33): New granular media permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VCAMSX"
        android:allowNativeHeapPointerTagging="false"
        tools:targetApi="34">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.VCAMSX">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Xposed Module Metadata -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="FaceGate: Advanced Camera Hook &amp; Spoofer" />
        <meta-data
            android:name="xposedminversion"
            android:value="93" />
        
        <!-- ✅ ADDED BACK: Default scope for common camera apps -->
        <!-- Users can still manually add more apps in LSPosed manager -->
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />
            
        <!-- Content Providers for Xposed Hook Data Sharing -->
        <provider
            android:name=".utils.VideoProvider"
            android:authorities="com.wangyiheng.vcamsx.videoprovider"
            android:exported="true"
            android:grantUriPermissions="true" />

        <provider
            android:name=".utils.MultiprocessSharedPreferences"
            android:authorities="com.wangyiheng.vcamsx.preferences"
            android:exported="true" />

    </application>

</manifest>